function hetdraw_msg1_HGUI
    d = dialog('Position',[600 600 300 150],'Name','Attention');

    txt = uicontrol('Parent',d,...
               'Style','text',...
               'Position',[15 70 280 70],...
               'String','Heterodomains were generated across a limited collector section in order to avoid display performance issues. Trajectory simulations use complete heterogeneous collector surface. Heterodomain sizes are exaggerated for presentation purposes only.'); 
    btn = uicontrol('Parent',d,...
               'Position',[115 20 70 25],...
               'String','OK',...
               'Callback','delete(gcf)');       
    % Get the handles of all pushbuttons and radiobuttons
    ContButton = [findall(gcf,'Style','Pushbutton');findall(gcf,'Style','radiobutton')];
    % Change to red all these buttons
    set(ContButton,'Backgroundcolor','r');
%     % Set to green the current button
%     set(gcbo,'Backgroundcolor','g');
end
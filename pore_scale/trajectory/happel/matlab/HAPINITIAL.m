%     SUBROUTINE TO OBTAIN INITIAL PARTICLE LOCATIONS
function [XINIT,YINIT,ZINIT,RINJ,HINIT]=HAPINITIAL (RLIM,RB,AG,AP)   
      RINJ = 2.0*RLIM; %INITIALIZE VALUE TO EXECUTE for WHILE LOOP
      while (RINJ>RLIM)
          XINIT = (rand*2-1)*RLIM;  
          YINIT = (rand*2-1)*RLIM; 
          RINJ = (XINIT^2.0 + YINIT^2.0)^0.5;
      end 
%     PROJECT INJECTION PLANE COORDINATES TO FLUID SHELL ENVELOPE
      ZINIT = (RB*RB-XINIT*XINIT-YINIT*YINIT)^0.5;
      RINIT = (XINIT^2.0 + YINIT^2.0 + ZINIT^2.0)^0.5;
      HINIT = RINIT-AG-AP;
end